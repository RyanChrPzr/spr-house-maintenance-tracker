name: spr_house_maintenance_tracker
description: "SPR House Maintenance Tracker — Filipino homeowners + home service vendors."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.12.0
  firebase_core: ^4.5.0
  firebase_messaging: ^16.1.2
  flutter_riverpod: ^2.6.1
  go_router: ^14.6.3
  image_picker: ^1.1.2
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  mocktail: ^1.0.4

flutter:
  uses-material-design: true

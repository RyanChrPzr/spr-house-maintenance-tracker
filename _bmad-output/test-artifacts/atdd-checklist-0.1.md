---
stepsCompleted: ['step-01-preflight-and-context', 'step-02-generation-mode', 'step-03-test-strategy', 'step-04-generate-tests', 'step-04c-aggregate', 'step-05-validate-and-complete']
lastStep: 'step-05-validate-and-complete'
lastSaved: '2026-03-04'
workflowType: 'testarch-atdd'
story_id: '0.1'
inputDocuments:
  - _bmad-output/planning-artifacts/epics.md
  - _bmad-output/planning-artifacts/architecture.md
  - _bmad-output/test-artifacts/test-design-epic-all.md
---

# ATDD Checklist — Epic 0, Story 0.1: Flutter Project Initialisation & Backend Infrastructure

**Date:** 2026-03-04  |  **Author:** HackathonTeam2  |  **Level:** Unit + Widget + Integration

---

## Story Summary

As the development team, we need the Flutter project created with all required dependencies
and the Supabase backend provisioned with the full 10-migration schema, so that all feature
development can begin without infrastructure blockers.

---

## Acceptance Criteria

1. **AC1** — `flutter create` produces a project that compiles and launches on iOS and Android
2. **AC2** — `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `FCM_SENDER_ID` accessible via `String.fromEnvironment()`; `.env` gitignored
3. **AC3** — Migrations 001–010 run via `supabase db push` → all 8 tables + RLS + pg_cron exist *(infrastructure check — no Flutter test)*
4. **AC4** — `lib/core/` contains: `app_exception.dart`, `app_constants.dart`, `app_theme.dart`, `app_router.dart` (go_router skeleton), `ErrorStateWidget`
5. **AC5** — `lib/features/{auth,property,maintenance,booking,vendor,notifications,payment}/` each have `data/`, `domain/`, `presentation/` subdirectories

---

## Failing Tests Created (RED Phase)

### Unit Tests — 2 tests
**File:** `test/core/app_exception_test.dart`
- ✅ `[P0] 0.1-UNIT-001` — AppException carries message + optional code | RED — `app_exception.dart` not created
- ✅ `[P1] 0.1-UNIT-002` — AppConstants.calmBlue == #2E6BC6 | RED — `app_constants.dart` not created

### Widget Tests — 1 test
**File:** `test/core/widgets/error_state_widget_test.dart`
- ✅ `[P1] 0.1-WIDGET-001` — ErrorStateWidget shows message + Retry; callback fires | RED — `error_state_widget.dart` not created

### Integration Tests — 1 test
**File:** `integration_test/setup_test.dart`
- ✅ `[P0] 0.1-INT-001` — App boots; unauthenticated user lands on /auth/login | RED — `main.dart` not created

---

## Required Widget Keys
- `Key('login_button')` — on LoginScreen (AC4 router skeleton)
- `Key('homeowner_dashboard')` / `Key('vendor_dashboard')` — absence verified on boot

## Implementation Checklist

### 0.1-UNIT-001 / 0.1-UNIT-002
- [ ] Create `lib/core/exceptions/app_exception.dart` with `AppException(String message, {String? code})`
- [ ] Create `lib/core/app_constants.dart` with `calmBlue = Color(0xFF2E6BC6)`, `trustBlue = Color(0xFF1B3A6B)`
- [ ] Remove stub classes from `app_exception_test.dart`; uncomment production imports
- [ ] Remove `skip:` from both unit tests
- [ ] `flutter test test/core/app_exception_test.dart`

### 0.1-WIDGET-001
- [ ] Create `lib/core/widgets/error_state_widget.dart` — `Column(Text(message), ElevatedButton('Retry', onRetry))`
- [ ] Remove stub from `error_state_widget_test.dart`; uncomment production import
- [ ] Remove `skip:` from widget test
- [ ] `flutter test test/core/widgets/error_state_widget_test.dart`

### 0.1-INT-001
- [ ] Run `flutter create spr_house_maintenance_tracker --org com.spr --platforms ios,android`
- [ ] Add deps to `pubspec.yaml`: `supabase_flutter ^2.12.0`, `firebase_messaging ^16.1.2`, `flutter_riverpod ^2.x`, `go_router ^14.x`, `image_picker ^1.x`; add `integration_test` + `mocktail` as dev deps
- [ ] Create `lib/main.dart` with `Supabase.initialize()` + `runApp(ProviderScope(child: MyApp()))`
- [ ] Create `lib/core/app_router.dart` (go_router skeleton with `/auth/login` redirect for unauthenticated)
- [ ] Create `lib/core/app_theme.dart` (`ColorScheme.fromSeed(seedColor: Color(0xFF1B3A6B))`)
- [ ] Create feature folder structure: `lib/features/{auth,property,maintenance,booking,vendor,notifications,payment}/{data,domain,presentation}/`
- [ ] Uncomment `app.main()` + assertions in `setup_test.dart`; remove `skip:`
- [ ] `flutter test integration_test/setup_test.dart --dart-define=... -d <device>`

### AC3 (Infrastructure — no Flutter test)
- [ ] Create Supabase project; note URL + anon key into `.env`
- [ ] Add `.env` to `.gitignore`
- [ ] Write migrations 001–010 in `supabase/migrations/`; run `supabase db push`
- [ ] Verify tables + RLS + pg_cron via Supabase dashboard

---

## Running Tests
```bash
flutter test test/core/app_exception_test.dart
flutter test test/core/widgets/error_state_widget_test.dart
flutter test integration_test/setup_test.dart --dart-define=SUPABASE_URL=<url> --dart-define=SUPABASE_ANON_KEY=<key> -d <device>
```

---

**Generated by BMad TEA Agent (Murat)** — 2026-03-04

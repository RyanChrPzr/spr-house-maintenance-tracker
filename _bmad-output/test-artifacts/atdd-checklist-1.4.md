---
stepsCompleted: ['step-01-preflight-and-context', 'step-02-generation-mode', 'step-03-test-strategy', 'step-04-generate-tests', 'step-04c-aggregate', 'step-05-validate-and-complete']
lastStep: 'step-05-validate-and-complete'
lastSaved: '2026-03-04'
workflowType: 'testarch-atdd'
story_id: '1.4'
inputDocuments:
  - _bmad-output/planning-artifacts/epics.md
  - _bmad-output/planning-artifacts/architecture.md
  - _bmad-output/test-artifacts/test-design-epic-all.md
---

# ATDD Checklist — Epic 1, Story 1.4: Property Settings Edit

**Date:** 2026-03-04  |  **Author:** HackathonTeam2  |  **Level:** Unit + Integration

---

## Story Summary

As a homeowner, I want to edit my property details (type and age) from settings so my
schedule stays accurate as my home's needs change. The change is saved to Supabase and
reflected immediately without a full app restart.

---

## Acceptance Criteria

1. **AC1** — Settings screen accessible from any homeowner screen; shows "My Property" section with current type and age
2. **AC2** — Update type or age + save → `properties` row updated → changes reflected immediately in app

---

## Failing Tests Created (RED Phase)

### Unit Tests — 1 test
**File:** `test/features/property/presentation/property_notifier_test.dart` (shared with Story 1.1)
- ✅ `[P1] 1.4-UNIT-001` — updateProperty patches Supabase `properties` row | RED

### Integration Tests — 1 test
**File:** `integration_test/property_settings_test.dart`
- ✅ `[P0] 1.4-INT-001` — Navigate to settings → edit property type + age → save → change shown | RED

---

## Required Widget Keys
| Key | Widget | Screen |
|---|---|---|
| `Key('settings_nav_item')` | BottomNavBar tab or IconButton | Any homeowner screen |
| `Key('my_property_section')` | ListTile / Card header | SettingsScreen |
| `Key('property_type_condo')` | Chip/Radio | PropertySettingsEditScreen |
| `Key('property_age_field')` | TextField | PropertySettingsEditScreen |
| `Key('property_settings_save')` | FilledButton | PropertySettingsEditScreen |
| `Key('property_type_display')` | Text | SettingsScreen (after save) |

---

## Implementation Checklist

### 1.4-UNIT-001
- [ ] Ensure `PropertyRepository.updateProperty(id, Property)` is implemented
- [ ] Ensure `PropertyNotifier.updateProperty()` calls repository and updates state
- [ ] Remove stub + uncomment production imports in `property_notifier_test.dart` (shared file)
- [ ] Remove `skip:` from `1.4-UNIT-001` only
- [ ] `flutter test test/features/property/presentation/property_notifier_test.dart --name "1.4"`

### 1.4-INT-001
- [ ] Create `lib/features/property/presentation/property_settings_screen.dart` showing current property details with `Key('my_property_section')`
- [ ] Add settings navigation: `Key('settings_nav_item')` in homeowner bottom nav or AppBar leading to SettingsScreen
- [ ] Wire edit form (reuse PropertySetupScreen widgets if possible) with `Key('property_type_condo')`, `Key('property_age_field')`, `Key('property_settings_save')`
- [ ] On save, `PropertyNotifier.updateProperty()` → optimistic UI update
- [ ] Seed test Supabase: homeowner with existing property (type: house, age: 5)
- [ ] Uncomment test body in `property_settings_test.dart`; remove `skip:` from INT-001

---

## Running Tests
```bash
flutter test test/features/property/presentation/property_notifier_test.dart --name "1.4"
flutter test integration_test/property_settings_test.dart --dart-define=SUPABASE_URL=<url> --dart-define=SUPABASE_ANON_KEY=<key> -d <device>
```

---

**Generated by BMad TEA Agent (Murat)** — 2026-03-04
